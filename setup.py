from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='sample project repo strcture that can be cloned from github.',
    author='Jay',
    license='MIT',
)